# --- dependencies / build ---
node_modules/
npm-debug.log*
*.log

# --- secrets & credentials (NEVER commit) ---
.env
.env.*
!.env.example
.auth/
*.pem
*.key
*_rsa
*.p12
credentials.json
client_secret*.json
service-account*.json
gha-creds-*.json

# --- local runtime / temp state ---
.DS_Store
Thumbs.db
build/
dist/
tmp/
scratchpad/
# local per-user run history (durable store is a GCS volume in the cloud)
.data/

# --- editor / AI-assistant scaffolding (not part of the app) ---
.claude/
CLAUDE.md
