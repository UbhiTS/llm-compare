{
  "name": "llm-agent-arena",
  "version": "1.0.0",
  "description": "Run the same agentic coding task against three LLMs in parallel and compare quality, speed, tokens and cost.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/mock-run.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
